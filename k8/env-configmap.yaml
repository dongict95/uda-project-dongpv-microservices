apiVersion: v1
kind: ConfigMap
data:
  AWS_BUCKET: s3-uda-dongpv4
  AWS_PROFILE: default
  AWS_REGION: us-east-1
  JWT_SECRET: testing
  POSTGRES_DB: postgres
  POSTGRES_HOST: host
  URL: http://localhost:8100  
metadata:
  name: env-config